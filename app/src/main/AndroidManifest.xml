<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.viacaoasteroide.app">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".PerfilActivity"></activity>
        <activity android:name=".PerfilDadosActivity"></activity>
        <activity android:name=".ViagemActivity"></activity>
        <activity android:name=".EditarDados"></activity>
        <activity android:name=".CompraActivity"></activity>
        <activity android:name=".QrCodeActivity"></activity>

    </application>
</manifest>
